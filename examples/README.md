# Examples

The crate binaries provide bounded examples:

```text
cargo run --locked --bin osia-core-r1-demo
cargo run --locked --bin osia-core-r1-replay
cargo run --locked --bin osia-core-r1-ledger-verify
```

They run only as user-space demonstrations. They do not configure a kernel, VM, device, or production system.
