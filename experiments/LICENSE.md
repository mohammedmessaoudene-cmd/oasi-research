# Experiment licenses

- `common/oasi_broker.py`, both relocatable Linux/WSL `experiment.py` files, detached
  aggregate checkers, red/green tests, and unit tests: `Apache-2.0`.
- Protocols, reports, JSON/JSONL data, manifests, environment metadata, data
  dictionary, and explanatory documentation: `CC-BY-4.0`.

The embedded RFC 8032 seed is a published test fixture, not a secret or a
production key. The Python package `cryptography` is a runtime dependency and
is not vendored in this repository.
